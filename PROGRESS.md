# Ход работ

**Где я сейчас:** 25 сентября 2026. Фаза 12 закрыта: дополнительные поля профиля —
хранилище с проверками в базе (D31), поля в профиле и в мастере заселения, экран
объявления у суперадмина, токены договора `profile.<код>`, приёмка. Прогон приёмок
снова зелёный — впервые с 21 сентября; разобраны все поломки, которые трое суток
скрывала немота входа (I24, I25). **Заблокировано:** ничего.
**CI по развёрнутому коммиту:** печатает `pnpm ci:status` — коммит, совпадает ли
он с боевым, и результат каждого прогона. Отчёт без этой строки недействителен
(CLAUDE.md §2, правило увиденного отказа).
**Развёртывание:** `scripts/deploy.sh` — собирает, применяет миграции, сверяет их число,
пересоздаёт контейнер и сверяет хеш коммита из `/api/health` плюс открывает страницу.
Версия видна снаружи: `curl https://nice.aqy.kz/api/health` → `commit` и `schema`.
**VPS (hoster.kz):** боевая версия сверяется `pnpm ci:status` (не переписывать хеш руками),
postgres только на 127.0.0.1, 33 миграции, снаружи только caddy
(D16), `https://nice.aqy.kz`. Бэкапы (D17, D18) ежедневно 03:30 Алматы на Drive.
Перенос с Supabase отменён (D19); утечка `.env.save` (I18) закрыта (D20).
**Не сделано:** расшифровка копии владельцем (`docs/BACKUP.md`), «Завести проживание» (P9-3).
**Разборы недели:** I19 (сборка вне проверок), I20 (сервис полномочий без тестов),
I21 (health не видел схемы), I24 (приёмки падали трое суток от лимита входов),
I25 (поле телефона затирало вставленный номер; нашлось, когда отказ назвал причину) —
все про утверждения, означавшие меньше, чем говорят.

**Не проверено живьём:** загрузка в Drive из браузера (I11), подача отсутствия (I15).

## Состояние по фазам

| Фаза | Состояние |
|---|---|
| 0–1. Каркас, пользователи, права, аудит | закрыты 6 сентября 2026 |
| 2–7. Заселение, деньги, ротации, присутствие, дэшборды, API | закрыты 7 сентября 2026 |
| 8. Настройки сети | закрыта 8 сентября 2026 |
| 9. Первый прогон на боевой | закрыта 9 сентября 2026 |
| 10. Переделка ротаций | закрыта 11 сентября 2026 |
| 11. Договор, инвентарь, временные жильцы | закрыта 25 сентября 2026 |
| 12. Дополнительные поля профиля | закрыта 25 сентября 2026 |

Чек-листы — `docs/tasks/PHASE-N.md`, решения — `docs/08-DECISIONS.md`, история — `git log`.

## Долги, которые переживают фазы

Каждый требует того, чего нет у разработки: ключа, живой учётной записи или телефона.

- **Казахская локаль не вычитана человеком**: полнота словарей под тестом, качество — нет.
- **Живьём не подтверждена** загрузка в Drive из браузера (I11).
- **Push не уходил живому сервису**: ключей VAPID нет. **WhatsApp остаётся заглушкой.**
- Мелочи — `docs/tasks/MAINTENANCE.md`.
