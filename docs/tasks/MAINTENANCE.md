# Обслуживание

Мелочи, которые не стоят прерывания работы. Разбираются в конце каждой фазы
одним коммитом (CLAUDE.md §8).

## Открыто

- [ ] **Молчаливый запасной адрес базы в интеграционных тестах.** В каждом файле
      `*.db-test.ts` стоит `process.env.TEST_DATABASE_URL ?? 'postgres://nice:nice@localhost:5432/nice_almaty'`.
      На машине разработчика порт 5432 занят другим сервером (это уже ловили в фазе 1),
      и прогон молча уходит туда: тот же класс ошибки, что инцидент I1. Свести выбор
      адреса в один помощник и падать с внятным текстом, когда переменная не задана.
      Заведено 7 сентября 2026 (фаза 2, T2.5b).

## Разобрано

- [x] **Депрекация Node 20 в GitHub Actions.** Заведено 6 сентября 2026 (фаза 1),
      разобрано в конце фазы 1: `actions/checkout` v4 → v7, `actions/setup-node` v4 → v7,
      `pnpm/action-setup` v4 → v6, `actions/upload-artifact` v4 → v7. Предупреждение
      о принудительном запуске на Node 24 в каждом прогоне снято.
