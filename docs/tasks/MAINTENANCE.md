# Обслуживание

Мелочи, которые не стоят прерывания работы. Разбираются в конце каждой фазы
одним коммитом (CLAUDE.md §8).

## Открыто

- [ ] **Решения тасков T3.1–T3.3 не записаны в `docs/08-DECISIONS.md`.** Заведено
      7 сентября 2026 (фаза 3, T3.4): раздел «Решения фазы 3» начинается сразу с P3-1,
      хотя разбор фазы обещал запись в том же коммите, где решение принято.
      Открытый вопрос 1 из `PHASE-3.md` — «чем считать дни проживания в месяце
      и как быть с днём выезда» — решён в коде `src/domain/utilities.ts`, но следа
      в документации не оставил. Восстановить из кода на закрытии фазы (T3.11),
      нумерацию продолжить с P3-10.

- [ ] **Список пользователей не разбит на страницы.** Заведено 7 сентября 2026
      (фаза 3, T3.12): экран `/settings/users` рисует все учётные записи сети разом.
      На накопленных прогоном шестистах жильцах перерисовка перестала укладываться
      в ожидание приёмки (инцидент I3); в живой сети столько наберётся за пару лет.
      Компонент `src/components/ui/pagination.tsx` есть с фазы 0 и не используется.

## Разобрано

- [x] **Молчаливый запасной адрес базы в интеграционных тестах.** Заведено 7 сентября 2026
      (фаза 2, T2.5b), разобрано в конце фазы 2: адрес берётся из `testDatabaseUrl()`,
      запасного значения нет — без переменной прогон падает с внятным текстом.
      Порт 5432 на машине разработчика занят чужим сервером, и молчаливый переход туда
      успел стать инцидентом I2. Сторож `src/db/testing/database-url.test.ts` не даёт
      запасному адресу вернуться ни в один `*.db-test.ts`.

- [x] **Депрекация Node 20 в GitHub Actions.** Заведено 6 сентября 2026 (фаза 1),
      разобрано в конце фазы 1: `actions/checkout` v4 → v7, `actions/setup-node` v4 → v7,
      `pnpm/action-setup` v4 → v6, `actions/upload-artifact` v4 → v7. Предупреждение
      о принудительном запуске на Node 24 в каждом прогоне снято.
