# Обслуживание

Мелочи, которые не стоят прерывания работы. Разбираются в конце каждой фазы
одним коммитом (CLAUDE.md §8).

## Открыто

- [ ] **Депрекация Node 20 в GitHub Actions.** `actions/checkout@v4`,
      `actions/setup-node@v4` и `pnpm/action-setup@v4` объявлены под Node 20
      и принудительно исполняются на Node 24. Прогон зелёный, но предупреждение
      висит в каждом запуске. Поднять версии, когда будет ясно, что все три
      действия имеют совместимый мажор. Заведено 6 сентября 2026, фаза 1.

## Разобрано

Пока ничего.
