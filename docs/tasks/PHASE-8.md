# Фаза 8. Настройки сети: типы документов, план счетов, шаблон договора

Источник: `docs/04-MODULES/11-users-settings.md` («Настройки сети»),
`docs/04-MODULES/10-accounting-inventory.md` («Учёт»),
`docs/OWNER-NOTES.md` (указание владельца от 8 сентября 2026).

Фаза закрывает дыру, вскрывшуюся на очистке боевой базы: три сущности из ТЗ
заводит только сид, а экранов у них нет. Порядок задан владельцем: типы
документов, план счетов, шаблон договора последним — он тянет версионирование.

Правило прежнее: один таск = один коммит = зелёный `pnpm verify`.

## Критерии приёмки фазы

- [ ] Суперадмин заводит тип документа, меняет срок годности и архивирует его
      без обращения к базе
- [ ] Суперадмин видит план счетов с остатками, заводит и переименовывает счёт,
      архивирует несистемный
- [ ] Суперадмин правит шаблон договора, вставляет токен кликом, видит
      предпросмотр на тестовых данных и сохраняет новую версию
- [ ] Договор, собранный по старой версии шаблона, остаётся читаемым:
      подписанные договоры не меняются задним числом
- [ ] Три поля под договор заполняются при заселении и попадают в PDF
- [ ] `pnpm db:seed --skeleton` на чистой базе и экраны фазы дают один и тот же
      результат: сеть, готовую к работе без единого запроса к базе руками

## Чего в этой фазе нет

Дополнительных полей профиля, объявляемых суперадмином (оценка — в
`docs/OWNER-NOTES.md`, решения владельца пока нет). Загрузки готового договора
в docx/pdf как вложения-образца: модуль 11 допускает её как справочную,
подставляемый документ всё равно собирается из HTML-шаблона.

## Задачи

### T8.1 — Три поля под договор
- [x] Миграция `0019`: `resident_profiles.id_doc_issuer` (по умолчанию «МВД РК»),
      `resident_profiles.registration_address`, `residencies.contract_number`
      с уникальным индексом по сети; применяется на чистой базе
- [x] Номер договора присваивает система: формат «ГГГГ-НННН», сквозная нумерация
      в пределах сети, каждый год заново (P8-1). Чистая функция
      `src/domain/contract-number.ts` с юнит-тестами, выдача — при заведении
      проживания, а старым проживаниям номер достаётся при первой сборке договора
- [x] Поля в профиле жильца, три локали
- [x] Новые токены шаблона: `resident.id_doc_issuer`,
      `resident.registration_address`, `residency.contract_number`
- [x] Базовый шаблон в сиде дополнен новыми токенами
- [x] Мутация пишет в `audit_log`: номер уходит в запись о заведении проживания
      и в запись о сборке договора
- Коммит: `feat(residents): орган выдачи, адрес прописки и номер договора`

### T8.2 — Экран типов документов
- [x] Сервис `src/services/document-types.ts`: создание, переименование
      (три локали), срок годности, флаг «нужна дата выдачи», порядок, архивация
- [x] Права: `settings.org.write` на правку и `settings.org.read` на чтение —
      отдельного действия не понадобилось, точка проверки одна
- [x] Архивация вместо удаления: на тип ссылаются загруженные документы.
      Код не меняется вовсе — он записан в путях уже загруженных файлов
- [x] Экран `/settings/document-types` и карточка в списке настроек,
      три ширины проверены приёмкой `e2e/phase-8.spec.ts`
- [x] Негативные фикстуры: админ получает отказ в сервисе (четыре вызова)
      и уезжает со страницы на `/settings` — проверено на трёх ширинах
- [x] Мутации пишут в `audit_log`: `document_type.saved` и `document_type.archived`
- Коммит: `feat(settings): типы документов`

### T8.3 — Экран плана счетов
- [x] Сервис `src/services/chart-of-accounts.ts`: список с остатками из проводок,
      создание, переименование, архивация
- [x] Системные счета не архивируются (§10.1): кнопки нет на экране, отказ в сервисе
- [x] Фонд дома заводится вместе с домом (`createHouse`) и в списке помечен домом
- [x] Экран `/settings/accounts` и карточка в списке настроек, три ширины
      проверены приёмкой
- [x] Негативные фикстуры: админ получает отказ во всех четырёх вызовах сервиса
      и уезжает со страницы на `/settings`; руками заводятся только касса,
      Kaspi и общий счёт — фонды появляются вместе с сетью и домом (P8-4)
- [x] Мутации пишут в `audit_log`: `account.saved` и `account.archived`
- Коммит: `feat(settings): план счетов`

### T8.4 — Редактор шаблона договора
- [x] Экран `/settings/contract-template`: текст договора и палитра из тринадцати
      токенов, вставка кликом в позицию курсора
- [x] Предпросмотр на выдуманных данных: образец лежит в домене рядом с палитрой,
      тест держит его полным — новый токен без значения оставил бы сырые скобки
- [x] Неизвестный токен не даёт сохранить: ошибка называет его по имени,
      проверка та же, что при сборке договора, но срабатывает раньше
- [x] Три локали, три ширины проверены приёмкой
- Коммит: `feat(settings): редактор шаблона договора`

### T8.5 — Версионирование шаблона
- [ ] Миграция: история версий шаблона, ссылка из `residencies` на версию,
      по которой собран договор
- [ ] Правка шаблона создаёт новую версию, старые остаются
- [ ] Подписанный договор ссылается на свою версию и не меняется задним числом
- [ ] Юнит-тесты подстановки на обеих версиях
- Коммит: `feat(contracts): версии шаблона договора`

### T8.6 — e2e приёмки фазы
- [ ] Суперадмин заводит тип документа и видит его в загрузке у жильца
- [ ] Суперадмин заводит счёт и видит его в плане счетов с нулевым остатком
- [ ] Суперадмин правит шаблон, собирает договор, правит снова — прежний
      договор не изменился
- [ ] Дом на каждую ширину свой, как в приёмках прошлых фаз
- Коммит: `test(e2e): приёмки фазы 8`

### T8.7 — Закрытие фазы
- [ ] `docs/08-DECISIONS.md` дополнен по ходу фазы
- [ ] `PROGRESS.md` обновлён
- [ ] `docs/tasks/MAINTENANCE.md` разобран
- [ ] Все чекбоксы закрыты
- Коммит: `docs: закрытие фазы 8`
