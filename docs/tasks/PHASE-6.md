# Фаза 6. Дэшборды, уведомления, PWA, инвентарь — чек-лист задач

Источник: `docs/07-ROADMAP.md` (Фаза 6), `docs/04-MODULES/09-dashboards.md`,
`docs/04-MODULES/10-accounting-inventory.md` (раздел «Инвентарь»),
`docs/02-DATA-MODEL.md` (разделы «Инвентарь», «Файлы, уведомления, система»),
`docs/01-ARCHITECTURE.md` (планировщик, адаптеры `notify/*`).

Правило прежнее: один таск = один коммит = зелёный `pnpm verify`.

## Критерии приёмки фазы (из роадмапа)

- [ ] Приложение устанавливается на телефон
- [ ] Push о неподтверждённой ротации приходит
- [ ] Все задания идемпотентны: повторный вызов не дублирует

## Чего в этой фазе нет

API для ботов, OpenAPI и токены — фаза 7. WhatsApp остаётся заглушкой:
адаптер есть, канал не подключён (нужен внешний договор — это остановка
по контракту автономии §8, пункт 3). Экспорт CSV/XLSX — здесь, но только
для инвентаря и списков, которые уже есть; отчётность фазы 7 сюда не входит.

---

## Таски

### T6.1 — Схема уведомлений и подписок
- [x] `notifications`, `notification_outbox`, `push_subscriptions` по модели данных
- [x] Репозитории с фильтрацией по `org_id` и адресату
- [x] Миграция применяется на чистой базе
- Коммит: `feat(db): уведомления, очередь доставки и подписки на push`

### T6.2 — Ядро очереди уведомлений
- [x] Постановка уведомления: адресат, тип, тексты в трёх локалях, payload
- [x] Разбор очереди `notifications-dispatch`: попытки, статусы, ошибки
- [x] Идемпотентность: повторный разбор не отправляет дважды
- [x] Негативная фикстура: уведомление чужого жильца не читается
- Коммит: `feat(notifications): очередь доставки и разбор outbox`

### T6.3 — Адаптеры доставки
- [x] `inapp` — запись в `notifications`, счётчик непрочитанного
- [x] `webpush` — VAPID, приём подписки, отзыв на 410/404
      (кнопка в профиле — T6.4, service worker — T6.6)
- [x] `whatsapp.stub` — честная заглушка со статусом `skipped`
- [x] Ключи VAPID из `src/env.ts`, в `.env.example` и в архитектуре
- Коммит: `feat(notifications): inapp, web push и заглушка whatsapp`

### T6.4 — Центр уведомлений
- [x] Список с непрочитанным, отметка «прочитано», фильтр по типу
- [x] Управление подпиской на push из профиля
- [x] Экран на 375/768/1440 и в обеих темах (`e2e/notifications.spec.ts`)
- Коммит: `feat(notifications): центр уведомлений и подписка`

### T6.5 — Остальные задания планировщика
- [x] `rotations-remind` 09:00 и 19:00
- [x] `curfew-check` 23:05: список без уведомления — админу, без автосанкций
- [x] `utilities-remind` и `schedule-remind` 25 числа
- [x] `documents-expiry` и `deposit-refund-watch` 09:00
- [x] Каждое идемпотентно через `job_runs`
- Коммит: `feat(jobs): напоминания, отбой и сроки документов`

### T6.6 — PWA
- [x] `manifest.webmanifest`, иконки, установка на домашний экран
- [x] Service worker: офлайн-оболочка и приём push
- [x] Проверка установки в приёмке фазы (`e2e/pwa.spec.ts`)
- Коммит: `feat(pwa): манифест, service worker и установка`

### T6.7 — Дэшборд жильца
- [x] Ближайшая уборка с кнопкой подтверждения, отсутствие, счёт, депозит
- [x] Рейтинг одним числом (если не скрыт настройкой)
- [x] Блок «Требуется внимание»: документы и незакрытые шаги заселения
- Экран на трёх ширинах проверяет приёмка фазы (T6.12): заселённого жильца
  готовит она же, а второй такой сценарий удвоил бы прогон ради тех же шагов
- Коммит: `feat(dashboard): экран жильца`

### T6.8 — Дэшборд админа дома
- [x] «Сегодня»: ротации дня и блок «Требует решения»
- [x] Оплаты, долги, задачи «Удалёнка»
- [x] Рейтинг дома, статус коммуналки, заселение
- Дом без админа ведёт суперадмин: выбор дома тот же, что на других экранах
- Коммит: `feat(dashboard): экран админа дома`

### T6.9 — Дэшборд суперадмина
- [x] Сеть по домам: занятость, деньги, средний рейтинг и оценка уборок
- [x] Возвраты депозитов с обратным отсчётом
- [x] «Требует решения»: предложения скидок, штрафы до счёта, незакрытые периоды
- Коммит: `feat(dashboard): экран суперадмина`

### T6.10 — Инвентарь
- [ ] `inventory_items`, `inventory_movements`, `inventory_audits`, `inventory_audit_lines`
- [ ] Приход, перемещение между домами, списание с историей движений
- [ ] Админ ведёт только свой дом; негативная фикстура на чужой
- Коммит: `feat(inventory): позиции, движения и права`

### T6.11 — Инвентаризация и экспорт
- [ ] Ведомость «по учёту / по факту / расхождение / комментарий»
- [ ] Закрытие с автоматической корректировкой количеств
- [ ] Экспорт CSV/XLSX списков инвентаря
- Коммит: `feat(inventory): ведомость, закрытие и экспорт`

### T6.12 — e2e приёмки фазы
- [ ] Приложение устанавливается на телефон (манифест и service worker)
- [ ] Push о неподтверждённой ротации доходит до центра уведомлений
- [ ] Повторный вызов задания ничего не дублирует
- Дом на каждую ширину свой, как в приёмках фаз 3, 4 и 5
- Коммит: `test(e2e): дэшборды, уведомления и инвентарь`

### Указания владельца (вне роадмапа, из `docs/OWNER-NOTES.md`)

#### T6.O1 — Проверка класса I6: доступ по идентификатору
- [x] Показано, чем закрыт I6: `админ без проживания подаёт не за жильца, а никак`
      (`src/services/absences.db-test.ts`) — админ дома, где есть заселённый жилец,
      получает отказ вместо чужой заявки
- [x] Сплошная проверка репозиториев на выборку по id без фильтра
- [x] Найденное починено: состояния порогов рейтинга (P6-15)
- Коммит: `fix(db): сеть у состояний порогов и устойчивый порядок списков`

#### T6.O2 — Проверка класса неустойчивого порядка
- [x] Все сортировки репозиториев дополнены уникальным ключом (P6-16)
- [x] Правило закреплено тестом, ловящим новую сортировку без ключа
- Коммит: тот же

#### T6.O3 — Дом без админа
- [x] Ротации, оценки, коммуналка и счета такого дома ведёт суперадмин
      (`src/services/house-without-admin.db-test.ts`)
- [x] Календарь ротаций и экран отсутствий дают суперадмину выбрать дом:
      раньше первый показывал пустоту, второй — первый попавшийся дом
- Коммит: `fix(rotations): суперадмин ведёт дом без админа`

#### T6.O4 — Оценка: отключаемые полномочия админа
- [x] Объём, место в плане и что ломается — ответом в `docs/OWNER-NOTES.md`
- Коммит: тот же

### T6.13 — Закрытие фазы
- [ ] `docs/08-DECISIONS.md` дополнен по ходу фазы
- [ ] `PROGRESS.md` обновлён
- [ ] `docs/tasks/MAINTENANCE.md` разобран
- [ ] Все чекбоксы закрыты
- Коммит: `docs: закрытие фазы 6`

---

## Что придётся решать самому

Перечень известен заранее; решения консервативные, каждое в `docs/08-DECISIONS.md`
в том же коммите, где принято.

1. **Где хранить ключи VAPID.** Секрет по §5 CLAUDE.md идёт через `src/env.ts`,
   но публичный ключ нужен браузеру — значит он не секрет и место у него другое.
2. **Что делать с подпиской, которую push-сервис отверг.** 410 означает «подписки
   больше нет», 404 — тоже; остальные коды — временная ошибка или настоящая.
3. **Сколько попыток даёт очередь и с какой паузой.** В модели есть `attempts`,
   но ни числа, ни паузы документация не называет.
4. **Показывать ли жильцу уведомления чужих домов после переезда.** История
   принадлежит человеку, а адрес — дому.
5. **Что считать «непрочитанным» на дэшборде.** Счётчик по всем каналам или
   только по `inapp`: push уже показан системой, и второй раз он не новость.
6. **Как экспортировать дробные количества инвентаря.** В модели `numeric(12,2)`,
   а деньги в системе целые: у инвентаря своя единица измерения, и правило §0
   к ней не применяется дословно.
