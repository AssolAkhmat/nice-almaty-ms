# Фаза 12 — дополнительные поля профиля

Собрана из указания владельца от 21 сентября 2026: «делаем по твоей оценке,
с уточнением: поля бывают обязательные и необязательные, каждое поле становится
токеном шаблона договора. Архивированное поле остаётся читаемым в уже подписанных
договорах». Оценка — `docs/OWNER-NOTES.md`, раздел «дополнительные поля профиля»:
пять тасков. Зависимость снята: версионирование шаблона (T8.5) закрыто.

Суть работы не в форме, а в том, что три вещи, известные сейчас на этапе сборки,
становятся данными: набор полей профиля, палитра токенов договора и правила
проверки. Порядок закрытия обычный: один таск — один коммит — зелёные тесты.

## T12.1 Хранилище объявленных полей

- [x] Миграция: `profile_field_defs` (`org_id`, `code`, `name_i18n`, тип,
      обязательность, порядок, `archived_at`) и `profile_field_values`
      (`user_id`, `field_id`, значение)
- [x] Отдельная таблица значений, а не JSONB в `resident_profiles`: правка
      значения пишется в `audit_log` построчно, по объявленному полю нужен поиск
- [x] Пять типов, а не «любой»: строка, число, дата, да/нет, выбор из списка.
      Негативная фикстура: шестой тип база не принимает
- [x] `code` уникален в сети и не переиспользуется после архивации: токен
      договора обязан значить одно и то же во всех версиях
- [x] Репозиторий `src/db/repositories/profile-fields.ts`, фильтр по `org_id`
- [x] Значение архивированного поля остаётся читаемым, запись нового отвергает
      база (триггер `profile_field_values_match_def`), а не только сервис

## T12.2 Поля в форме заселения и в профиле

- [ ] Поля рисуются по объявлению; порядок из `order`, названия из `name_i18n`,
      а не из словарей
- [ ] Схема zod собирается в рантайме по типу и обязательности
- [ ] Обязательность проверяется на сервере, а не только в браузере:
      негативная фикстура — запрос мимо формы без обязательного поля
- [ ] Правка значения пишет `audit_log` с кодом поля в `entity_id`-записи
- [ ] Архивированное поле в форме не появляется, но показывается в карточке
      как прежнее значение
- [ ] 375/768/1440, обе темы, три локали

## T12.3 Экран объявления полей у суперадмина

- [ ] Список, создание, переименование, изменение порядка, архивация
- [ ] Архивация вместо удаления: значения остаются в договорах и в истории.
      Кнопки удаления нет вовсе
- [ ] Право `profileField.write` только у суперадмина; админу экран не открывается
- [ ] Каждая мутация в `audit_log`
- [ ] Предупреждение при архивации поля, которое используется действующим
      шаблоном договора: токен станет пустым

## T12.4 Токены шаблона `profile.<код>`

- [ ] `unknownTokens()` принимает палитру аргументом, а не импортирует константу:
      правка во всех вызовах и тестах
- [ ] Палитра действующего шаблона = константы + объявленные поля сети
- [ ] Предпросмотр на тестовых данных: образец значения на каждый из пяти типов
- [ ] Подписанный договор собирается по своей версии шаблона и по значениям
      на момент подписания: архивация поля его не меняет. Интеграционная фикстура
- [ ] Спецификация OpenAPI профиля: объявленные поля отдаются описанием,
      собранным по объявлениям, — бот не теряет типы
- [ ] Решение в `docs/08-DECISIONS.md`

## T12.5 Приёмка

- [ ] Суперадмин объявляет обязательное поле — оно требуется при заселении
- [ ] Значение попадает в собранный договор через токен
- [ ] После архивации поля старый договор собирается по-прежнему, а новый —
      без токена
- [ ] Прогон на трёх ширинах; правки объявлений идут на одной, как шаблон
      и рассылка: объявление в сети одно
