# Фаза 9. Первый прогон на боевой: блокеры и дефекты

Источник: список владельца от 9 сентября 2026 (первый прогон на боевой базе),
`docs/03-BUSINESS-RULES.md`, `docs/04-MODULES/*`, `docs/05-DESIGN-SYSTEM.md`,
`docs/08-DECISIONS.md` (D11, D4, инцидент I9 — как не выдавать догадку за причину).

Порядок задан владельцем: блокеры 1-4, дефекты 5-13. Переделка ротаций —
отдельной фазой 10, и начинается она с модели на согласование, не с кода.

Правило прежнее: один таск = один коммит = зелёный `pnpm verify`.
Где ТЗ молчит — консервативный вариант и `[ОТКРЫТО]` в `docs/08-DECISIONS.md`.
Что требует ключей владельца — запись в `docs/BLOCKED.md`, следующий таск.

## Критерии приёмки фазы

- [ ] Загрузка файла из браузера в Google Drive доходит до `ready`: причина
      отказа найдена по коду и документации Drive, исправление под тестами,
      в `PROGRESS.md` записано, что проверено и что проверить без ключей нельзя
- [ ] Договор собирается на боевой; печать PDF в проде проверена или её
      невозможность записана с причиной
- [ ] `/profile` открывается у жильца
- [ ] Админу назначается место (D11)
- [ ] Переключение локали меняет язык интерфейса на всех трёх
- [ ] Выход из системы есть на экране
- [ ] Номер телефона меняется через экран с записью в `audit_log`
- [ ] Все экраны всех ролей переживают пустую базу; e2e-приёмка на пустой базе

## Задачи

### T9.1 — Загрузка файлов: прямая загрузка в Drive из браузера
- [x] Разбор двухшагового потока по коду и документации Google:
      сессия открывается сервером без заголовка `Origin`, ответ Drive
      на `PUT` из браузера не несёт CORS-заголовков — браузер прячет ответ
      за `Failed to fetch` (инцидент I11, разбор в `docs/08-DECISIONS.md`)
- [x] Тест-первым: `UploadMeta.origin`, драйвер `gdrive` передаёт `Origin`
      при открытии сессии, роут берёт его из запроса браузера (P9-1)
- [x] Сервисы и роут прокидывают origin; локальный драйвер его игнорирует
- [x] `PROGRESS.md`: что проверено, что проверить нельзя, признаки для
      различения версий в браузере — ссылкой на разбор I11
- Коммит: `fix(files): resumable-сессия Drive привязана к Origin браузера`

### T9.2 — Договор не формируется
- [x] Причина найдена по коду и сборке: на Vercel нет `@sparticuz/chromium` —
      по P2-16 он не входил в зависимости, а боевая собирается из репозитория
      (инцидент I12, P9-2)
- [x] Все server actions ведут сбои в журнал через `actionErrorKey`, под тестом;
      `AppError` по-прежнему уходит ключом на экран без записи
- [x] Пакет в зависимостях, литеральный импорт, `serverExternalPackages`,
      архивы `bin/**` в трассировке функции Vercel — проверено по `.nft.json`
      локальной сборки под `DEPLOY_TARGET=vercel`; в standalone для Docker исключён
- [x] `GET /api/health?pdf=1` проверяет печать на боевой без заведения проживания;
      живьём не проверено — превью-деплоя у разработки нет
- Коммит: `fix(contracts): печать на Vercel и журнал сбоев server actions`

### T9.3 — /profile не открывается
- [x] Причина найдена: место читалось прямым запросом к схеме дома, которой
      жилец не видит (P2-5) — экран падал сразу после назначения места (I13)
- [x] Место читается через своё проживание; проживание берётся по `userId`,
      а не первым видимым (I6)
- [x] Приёмка: жилец с назначенным местом видит его в профиле, три ширины
- Коммит: `fix(profile): место жильца читается через своё проживание`

### T9.4 — Админу назначается место (D11)
- [ ] Мастер заселения принимает админа как жильца дома
- [ ] Негативная фикстура: админ чужого дома не заселяется
- Коммит: `fix(residents): ...`

### T9.5 — Переключение локали
- [ ] Причина найдена; e2e на трёх локалях
- Коммит: `fix(i18n): ...`

### T9.6 — Кнопка выхода
- Коммит: `feat(auth): выход из системы`

### T9.7 — Смена номера телефона
- [ ] Свой номер — в личных настройках, чужой — суперадмину в карточке
- [ ] Запись в `audit_log`, три локали, три ширины
- Коммит: `feat(users): смена номера телефона`

### T9.8 — Вёрстка счетов
- Коммит: `fix(invoices): вёрстка`

### T9.9 — Общие зоны не показываются в разделе мест
- Коммит: `fix(beds): ...`

### T9.10 — Вкладка «Ещё»: меню не наезжает на вкладку
- Коммит: `fix(layout): ...`

### T9.11 — Favicon
- [ ] Домик `#004AAD` на `#FEE274`, без градиентов, SVG плюс ICO
- Коммит: `feat(pwa): favicon`

### T9.12 — Уведомление об отсутствии: падение и статус
- [ ] Причина падения найдена
- [ ] Долгосрочное отсутствие требует одобрения (§5)
- Коммит: `fix(absences): ...`

### T9.13 — Пустая база
- [ ] Все экраны всех ролей на пустой базе
- [ ] e2e-приёмка на пустой базе
- Коммит: `fix(app): экраны переживают пустую базу`

### T9.14 — Закрытие фазы
- [ ] `docs/08-DECISIONS.md`, `PROGRESS.md`, `MAINTENANCE.md`
- Коммит: `docs: закрытие фазы 9`
