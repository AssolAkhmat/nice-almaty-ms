# Фаза 1. Пользователи, права, аудит — чек-лист задач

Источник: `docs/07-ROADMAP.md` (Фаза 1), `docs/04-MODULES/11-users-settings.md`,
`docs/01-ARCHITECTURE.md` (разделы «Аутентификация», «Безопасность данных»),
`docs/02-DATA-MODEL.md`, `docs/00-PRD.md` (роли).

Правило прежнее: один таск = один коммит = зелёный `pnpm verify`.

## Критерии приёмки фазы (из роадмапа)

- [ ] Суперадмин создаёт админа и жильца
- [ ] Админ не видит чужой дом — проверено e2e
- [ ] Каждое действие попадает в `audit_log`

---

## Таски

### T1.1 — Схема: организации, дома, пользователи, сессии, настройки, аудит
- [x] `organizations`, `houses`, `users`, `sessions`, `settings`, `audit_log` по `02-DATA-MODEL.md`
- [x] `rate_limits(key text primary key, window_start timestamptz, count int)` — таблицы
      не было в модели данных, вносится в `docs/02-DATA-MODEL.md` этим же таском (P1-2)
- [x] Enum-типы: `user_role`, `user_status`, `locale`, `settings_scope`
- [x] Индексы: `users.phone` уникальный, `audit_log (entity_type, entity_id)` и `created_at`,
      `settings (scope, scope_id, key)` уникальный
- [x] Ограничение: `house_id` обязателен для роли `admin` и пуст для остальных
- [x] Миграция генерируется и применяется на чистой БД
- [x] Инвариант `house_id` проверен на живой БД: админ без дома и жилец с домом
      отвергаются, админ с домом проходит. Автоматизация — в T1.3, вместе
      с интеграционными тестами репозиториев
- Коммит: `feat(db): организации, дома, пользователи, сессии, аудит`

### T1.2 — Телефон и пароли (TDD)
- [x] **Тесты вперёд**: нормализация в `+7XXXXXXXXXX` — формы `8XXXXXXXXXX`, `+7 (XXX) XXX-XX-XX`,
      `7XXXXXXXXXX`, мусор отвергается
- [x] `src/domain/phone.ts` — чистая функция нормализации и проверки
- [x] **Тесты вперёд**: правила пароля (P1-3) — 10..128 символов, запрет совпадения
      с телефоном, с временным паролем и с предыдущим
- [x] `src/lib/validation/password.ts` — одна zod-схема на клиент и сервер
- [x] `src/lib/password.ts` — argon2id: хеш, проверка, генерация временного пароля
- Коммит: `feat(domain): нормализация телефона и работа с паролями`

### T1.3 — Репозитории с обязательной фильтрацией
- [x] `src/db/repositories/*` для организаций, домов, пользователей, сессий, настроек, аудита
- [x] Каждый запрос принимает контекст доступа и фильтруется по `org_id`,
      для роли `admin` — дополнительно по `house_id`
- [x] Тест: запрос без контекста доступа не компилируется либо падает
- [x] Интеграционные тесты на настоящей БД: инварианты схемы из T1.1,
      включая `users_admin_has_house` и уникальность телефона
- Коммит: `feat(db): репозитории с фильтрацией по org_id и house_id`

### T1.4 — Матрица прав и `authz.ts`
- [ ] `src/lib/permissions.ts` — декларативные данные «роль × действие × область видимости»;
      `authz.ts` собственной логики не содержит и только читает их (P1-4)
- [ ] Тесты пишутся на **запреты**: на каждую отрицательную ячейку — свой тест
- [ ] Правило «роль и проживание — разные сущности» (D11): админ одновременно жилец
- [ ] Тест: перечень действий в матрице покрывает все роли без дыр
- [ ] Обращение к чужому дому или чужой организации даёт 404, а не 403 (P1-1);
      403 остаётся для запрещённого действия внутри своей области видимости
- Коммит: `feat(authz): матрица прав и единая точка проверки`

### T1.5 — `AuthProvider`: вход, сессии, смена пароля
- [ ] Интерфейс `signIn`, `getSession`, `revoke`, `setPassword`; драйвер `local`
- [ ] Сессия — строка в БД, в cookie непрозрачный токен (httpOnly, Secure, SameSite=Lax)
- [ ] TTL 30 дней со скользящим продлением
- [ ] Тест: отозванная сессия перестаёт действовать немедленно
- Коммит: `feat(auth): вход по телефону и сессии в базе`

### T1.6 — Ограничение попыток входа
- [ ] 10 попыток за 15 минут, ключи `login:phone:+7XXXXXXXXXX` и `login:ip:X.X.X.X` (P1-2)
- [ ] Просроченные строки удаляются лениво при записи; отдельная очистка — фаза 6
- [ ] **Тесты**: одиннадцатая попытка отклоняется; окно сдвигается; успешный вход сбрасывает счётчик
- Коммит: `feat(auth): ограничение частоты попыток входа`

### T1.7 — Аудит
- [ ] `src/services/audit.ts`: кто, когда, что, значения до и после, IP, `request_id`
- [ ] Перечень событий из `03-BUSINESS-RULES.md` §11, применимых к этой фазе
- [ ] Запись идёт в той же транзакции, что и изменение
- [ ] Тест: мутация без записи в аудит не проходит
- Коммит: `feat(audit): журнал действий`

### T1.8 — Экран входа и обязательная смена пароля
- [ ] `/login`: телефон и пароль, ошибки под полем, блокировка кнопки на время отправки
- [ ] `must_change_password` — до смены доступен только экран смены пароля
- [ ] Middleware: незалогиненный не попадает в `(app)`
- [ ] Все строки — ключи i18n в трёх локалях
- Коммит: `feat(auth): экраны входа и смены пароля`

### T1.9 — Разрешение сброса пароля
- [ ] Кнопка «Разрешить сброс» у админа и суперадмина
- [ ] Разрешение живёт 24 часа и одноразовое; вход с любым паролем и немедленная смена
- [ ] Каждое использование — в `audit_log`
- [ ] **Тесты**: разрешение истекает; второй раз не срабатывает
- Коммит: `feat(auth): одноразовое разрешение сброса пароля`

### T1.10 — Пользователи: список и карточка
- [ ] Список: телефон, ФИО, роль, дом, статус, последний вход; фильтры
- [ ] Создание аккаунта только суперадмином; временный пароль показывается один раз
- [ ] Действия: архивировать, сменить роль, перевести админа на другой дом
- [ ] Удаления нет — только архивация
- Коммит: `feat(users): список, создание и действия над аккаунтами`

### T1.11 — Дома
- [ ] Создание, переименование, адрес, комендантский час, депозит по умолчанию
- [ ] Архивация дома
- [ ] Админ видит только свой дом
- Коммит: `feat(houses): управление домами`

### T1.12 — Настройки
- [ ] Настройки сети и дома через таблицу `settings` (org / house)
- [ ] Личные настройки: язык, тема, смена пароля
- [ ] Язык и тема сохраняются в профиль — закрывает отложенное из фазы 0 (P0-2, P0-7)
- [ ] Видимость рейтинга у жильцов — только флаг настройки, механика в фазе 5
- Коммит: `feat(settings): настройки сети, дома и личные`

### T1.13 — Просмотр аудита
- [ ] Экран только для суперадмина: фильтры по актору, сущности, периоду
- [ ] Значения «до» и «после» показываются читаемо
- [ ] Экспорт CSV отложен до фазы 6 вместе с остальными выгрузками
- Коммит: `feat(audit): экран журнала для суперадмина`

### T1.14 — `StorageProvider`, драйвер `local`
- [ ] Интерфейс дополняется до рабочего: положить, отдать, удалить, проверка живости
- [ ] Драйвер `local` реализован полностью; `gdrive` и `supabase` — фаза 2
- [ ] Таблица `files` и двухшаговая загрузка — фаза 2, здесь только адаптер
- Коммит: `feat(storage): адаптер хранилища с локальным драйвером`

### T1.15 — Сид
- [ ] Одна сеть, пять домов, суперадмин, по одному админу на дом
- [ ] Идемпотентность: повторный запуск не плодит дубликаты
- [ ] Учётные данные печатаются в вывод `pnpm db:seed`
- [ ] Жильцы, зоны и места — фаза 2; ротации — фаза 4
- Коммит: `feat(db): сид сети, домов и учётных записей`

### T1.16 — e2e приёмки фазы
- [ ] Суперадмин создаёт админа и жильца
- [ ] Админ не видит чужой дом — ни в списках, ни по прямой ссылке
- [ ] Действия попадают в аудит
- [ ] Вход, обязательная смена пароля, разрешение сброса
- Коммит: `test(e2e): права и аудит`

### T1.17 — Закрытие фазы
- [ ] `docs/08-DECISIONS.md` дополнен по ходу фазы
- [ ] `PROGRESS.md` обновлён
- [ ] Все чекбоксы закрыты
- Коммит: `docs: закрытие фазы 1`

---

## Места, где документация молчит

Четыре закрыты владельцем до начала работ и записаны в `docs/08-DECISIONS.md`
как P1-1 … P1-4: код ответа на чужой дом, хранилище счётчика попыток,
требования к паролю, устройство матрицы прав.

Остаются четыре, решения по ним приняты и записываются в том же коммите,
где применяются:

1. **Cookie сессии и продление.** Имя `nice_almaty_session`, httpOnly, Secure,
   SameSite=Lax, Path=/. Внутри — непрозрачный токен, в базе только его хеш.
   TTL 30 дней; продление ленивое, при запросе не чаще раза в сутки,
   чтобы не писать в базу на каждый запрос.
2. **Состав `before`/`after` в аудите.** Пишутся только изменившиеся поля, а не вся
   строка. Зашифрованные поля (`iin_enc`, `id_doc_number_enc`) в журнал не попадают
   никогда — вместо значения пишется `"***"` и признак самого факта изменения.
   Раскрытие ИИН и УДЛ кнопкой «глаз» — отдельное событие аудита без значения.
3. **Временный пароль.** 12 символов из алфавита без похожих начертаний
   (без `0OoIl1`), генерация через `crypto.getRandomValues`. Показывается один раз,
   в базе только argon2id-хеш, в журнал аудита не попадает.
4. **Бутстрап первого суперадмина.** Только сид, никаких переменных окружения.
   `pnpm db:seed` создаёт суперадмина с временным паролем, печатает его один раз
   в вывод и ставит `must_change_password = true`.

## Порядок

T1.1 → T1.2 → T1.3 → T1.4 → T1.5 → T1.6 → T1.7 → T1.8 → T1.9 → T1.10 → T1.11
→ T1.12 → T1.13 → T1.14 → T1.15 → T1.16 → T1.17.

Схема и чистые функции идут первыми, экраны — после прав и аудита: иначе экраны
пришлось бы переписывать под матрицу прав.
