import Link from 'next/link';

/**
 * Негативная фикстура: прямой импорт `next/link` в компонентах запрещён —
 * предзагруженная страница показывает снимок, снятый до правки, и после
 * действия экран не обновляется. Ссылки идут через `@/components/ui/app-link`.
 *
 * Файл существует ради проверки правила: без него запрет однажды перестал бы
 * срабатывать, и никто бы этого не заметил (CLAUDE.md §2).
 */
export function DirectLinkFixture(): React.ReactElement {
  return <Link href="/">Ссылка мимо обёртки</Link>;
}
